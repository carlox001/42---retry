$"'HOME'" espansiano deve avvenire

/bins/ls no such file or directory

'$s' $HOME

cat su heredoc;

ls | pwd | cat | ciao

"$"


/*****************era il file che si stava modificando**********************/

/bins/ls no such file or directory (127) (risolto)

cat su heredoc; (ctrl c) (risolto?)

cat| cat | ls ha rotto il cazzo (risolto)

cat ctrl c 130 cazo (risolto)

# ls | pwd | cat | ciao (funziona)

echo '" "' (risolto)

echo -nnnn"nnn" (risolto)

echo -nnnnn ciao (risolto)

echo $_ (loop infinito, parsing) (risolto)

echo $-PWD (loop infinito, parsing) (risolto)

echo asdasdas (stampa spazio in piu) (risolto)

echo "$s" $HOME (stampa virgolette singole in piu) (risolto)

ctrl-c in HEREDOC -> se errno != EBADF eseguo comandi (altrimenti \
termino il ciclo degli heredoc e non eseguo comandi)

echo ciao > file (risolto)

$"HOME" (risolto) leakka;

" ' $HOME ' " / $_ deve espandere (RISOLVERE)

Cat ctrl c -> 130 (risolto)

/bins/ls no such file or directory (127) (risolto)

pippo+=bene (risolto)

Invio nella history (risolto)

Cat << hd ctrl c (completa il conando) 

Ctrl c >> a (130) (risolto)

cat << a << b | cat << c << d ctrlc non deve stampare shell (risolto)

Echo / builtin con le pipe PKEEEEEEEEEÈ (risolto) 

echo ciao "|" < a (risolto)

echo ciao | cat a (prende echo) (risolto)

echo ciao | cat<a invalid read (risolto)

Ciao >>> a / <<< a syntax error (risolto)

Echo > a con a senza permessi (dà errori) (risolto)

"$USER" -> cazeriniR (risolto)

./////.//././///.. (leak) (risolto)
./ciao
./minishell

cd .qualcosa si bugga un pochino

cat << a | cat <<b (scrive il primo al posto del secondo) (risolvi)

export VA"=''la' "y''olo (valido) (risolto)

export | cmd (leak, SIGPIPE) (non e' vero);

export | < a grep a / cat non funziona (le pipe / redir non funzionano) (risolto)(DOUBLE LEAK)

$? -> 1? (risolto)

echo test > $VAR (leak) (e seg fault) (risolto)
